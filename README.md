# 🤖 GPT-Fleet-Admin
> **The Ultimate Multi-Workspace Management & Automated Sales Bot for ChatGPT Business**

[![Python Version](https://img.shields.io/badge/python-3.10%2B-blue.svg)](https://www.python.org/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Framework: Aiogram 3](https://img.shields.io/badge/Framework-Aiogram%203-red.svg)](https://docs.aiogram.dev/)

**GPT-Fleet-Admin** یک راهکار جامع و فوق‌العاده سبک برای مدیرانی است که چندین ورک‌اسپیس چت‌جی‌پی‌تی بیزنس را مدیریت می‌کنند. این ربات با تمرکز بر امنیت (Zero-Port Exposure) و اتوماسیون فروش، تمام نیازهای شما را در یک پنل تلگرامی خلاصه می‌کند.

---

## ✨ ویژگی‌های کلیدی

### � پنل ادمین (مدیریت متمرکز)
- **مدیریت ورک‌اسپیس‌ها:** ثبت مشخصات لاگین، تاریخ انقضا و ظرفیت صندلی‌ها.
- **وارد کردن هوشمند (Smart Import):** استخراج خودکار لیست اعضا از متن کپی شده پنل OpenAI (بدون نیاز به API).
- **سیستم مالی خودکار:** تایید فیش‌های واریزی و ارسال آنی اکانت برای کاربر.
- **یادآورهای هوشمند (Reminders):** هشدار خودکار در فواصل ۷، ۳ و ۱ روز مانده به انقضای هر اکانت.
- **گزارش روزانه:** دریافت وضعیت کل سیستم هر روز صبح در تلگرام.
- **امنیت Admin-Only:** دسترسی کاملاً محدود به لیست سفید آیدی‌های تلگرام.

### 👤 پنل کاربر (خرید و تمدید)
- **مشاهده پکیج‌ها:** نمایش بسته‌های موجود با قیمت و توضیحات.
- **سیستم واریز:** ارسال تصویر فیش واریزی مستقیماً برای ادمین.
- **پنل کاربری:** مشاهده مشخصات اکانت تخصیص داده شده و زمان باقی‌مانده.

---

## 🛠 راهنمای نصب و راه‌اندازی (Ubuntu Server)

### ۱. پیش‌نیازها
مطمئن شوید که پایتون ۳.۱۰ یا بالاتر روی سرور نصب است:
```bash
sudo apt update && sudo apt install -y python3-pip python3-venv git
```

### ۲. دریافت پروژه
```bash
git clone https://github.com/tawanamohammadi/GPT-Fleet-Admin.git
cd GPT-Fleet-Admin
```

### ۳. آماده‌سازی محیط
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### ۴. پیکربندی (Configuration)
با اجرای اسکریپت راه‌انداز، توکن و آیدی ادمین خود را وارد کنید:
```bash
python setup.py
```

### ۵. اجرای دائمی (Systemd)
برای اینکه ربات با بستن ترمینال متوقف نشود و بعد از ریبوت سرور خودکار اجرا شود:
```bash
# ویرایش فایل سرویس (مسیرها را با مسیر پروژه خود ست کنید)
sudo nano gptadmin.service

# کپی در پوشه سیستم‌دی
sudo cp gptadmin.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable gptadmin
sudo systemctl start gptadmin
```

---

## 🔒 امنیت و عملکرد
- **Zero Port Mapping:** این ربات از `Long Polling` استفاده می‌کند و هیچ پورتی از سرور شما را اشغال نمی‌کند (سازگار با X-UI و وب‌سرورها).
- **SQLite Database:** دیتابیس محلی و سبک بدون نیاز به نصب موتورهای سنگین.
- **Low Resource Usage:** مصرف رم کمتر از ۱۰۰ مگابایت در حالت Idle.

---

## 📞 پشتیبانی و توسعه
طراحی شده با ❤️ برای مدیریت حرفه‌ای بیزنس‌های هوش مصنوعی.

[![GitHub Issues](https://img.shields.io/github/issues/tawanamohammadi/GPT-Fleet-Admin.svg)](https://github.com/tawanamohammadi/GPT-Fleet-Admin/issues)
