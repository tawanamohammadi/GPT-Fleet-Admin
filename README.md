# GPT Management & Sales Bot (Private Admin) 🤖

این ربات یک سیستم کامل برای مدیریت اکانت‌های ChatGPT Business و فروش اشتراک به کاربران است.

## ویژگی‌های بخش ادمین:
- 🔐 **امنیت ادمین:** فقط ادمین‌های لیست شده در `.env` دسترسی دارند.
- 📂 **مدیریت ورک‌اسپیس:** ثبت مشخصات لاگین (ایمیل و پسورد)، تاریخ انقضا و ظرفیت.
- 📥 **وارد کردن خودکار:** تشخیص اعضا از متن کپی شده از پنل OpenAI.
- 💳 **تایید فیش:** بررسی تصاویر فیش واریزی کاربران و تایید آن‌ها.
- 🚀 **ارسال خودکار:** بعد از تایید فیش، ادمین اکانت را انتخاب می‌کند و ربات مشخصات لاگین را برای کاربر می‌فرستد.
- ⏳ **ریماندر:** هشدار خودکار ۷، ۳ و ۱ روز مانده به انقضای هر ورک‌اسپیس.
- � **گزارش روزانه:** دریافت گزارش وضعیت کل سیستم هر روز صبح ساعت ۹.

## ویژگی‌های بخش کاربر:
- 🛍 **خرید/تمدید:** مشاهده پکیج‌ها و ارسال تصویر فیش واریزی.
- 👤 **اکانت من:** مشاهده ایمیل تخصیص یافته و زمان باقی‌مانده از اشتراک.

## نصب و راه‌اندازی (Ubuntu)

1. **نصب پیشنیازها:**
   ```bash
   sudo apt update
   sudo apt install python3-pip python3-venv
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   ```

2. **تنظیم فایل تنظیمات:**
   برای راحتی کار، اسکریپت راه‌اندازی را اجرا کنید:
   ```bash
   python setup.py
   ```
   این اسکریپت از شما توکن ربات و آیدی عددی ادمین را می‌پرسد و تنظیمات را انجام می‌دهد.

3. **اطلاعات پورت:**
   این ربات از روش Polling استفاده می‌کند و **هیچ پورتی** را روی سرور شما اشغال نمی‌کند. بنابراین با هیچ سرویس دیگری تداخل نخواهد داشت.

4. **اجرا:**
   ```bash
   python bot.py
   ```

4. **سیستم‌دی (برای اجرای دائمی):**
   فایل `gptadmin.service` را ویرایش کرده (مسیرها را درست کنید) و در `/etc/systemd/system/` کپی کنید:
   ```bash
   sudo systemctl enable gptadmin
   sudo systemctl start gptadmin
   ```

---
طراحی شده برای نظم‌دهی به بیزنس ChatGPT شما. 🚀
